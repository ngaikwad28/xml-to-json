# xml-to-json


# xml-to-json-plugin for Kong

## Overview

The `xml-to-json` for Kong Gateway provides conversion  functionalities for responses coming from backend.

## Installation

1. **Clone the repository:**

   ```bash
   git clone https://github.com/yourusername/xml-to-json-plugin.git
   cd json-to-xml-plugin



=========================================================================================
### 3. Install and Enable the Plugin

1. **Install the Plugin**

   If you have LuaRocks installed, navigate to the plugin directory and run:

   ```bash
   sudo luarocks make kong-plugin-xml-to-json-0.1.0-1.rockspec 


# Enable the Plugin

 ```bash
curl -X POST http://localhost:8001/services/{service_id}/plugins \
     --data "name=xml-to-json"








